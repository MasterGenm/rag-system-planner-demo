[project]
name = "rag-system-planner-demo"
version = "0.0.0"
requires-python = ">=3.11"

[project.scripts]
rag-planner = "cli.rag_planner:main"

[dependency-groups]
dev = [
  "pytest>=8.0",
  "jsonschema>=4.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
